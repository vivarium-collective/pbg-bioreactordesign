"""Custom bigraph-schema types for BiRD bioreactor processes.

Currently uses built-in types only. This function is a hook for
future custom type registrations (e.g., concentration units,
reaction network types).
"""


def register_bird_types(core):
    """Register custom types used by BiRD processes.

    Currently a no-op; provided as an extension point.
    """
    pass
